---
name: syn-trim-cot-leakage
description: 清理 Synlen 仓库中"会话视角泄漏"的文字（注释、dartdoc、docs/、Agent Notes）——写作会话残留、PR 叙事、评审辩解等八类。当审计注释或文档发现"读起来像聊天记录"时触发，配合 syn-prose-standard 使用。
---

# 清理会话视角泄漏

## 1. 判定标准

对每段可疑文字问：**一个只看当前 HEAD、接触不到任何对话记录、PR 讨论、issue 评论的读者，能否解析所有引用、验证所有断言？**

- 能 → 合规（写得啰嗦交给 `syn-prose-standard`）。
- 不能但含事实 → 把事实改写为站在仓库视角的**现在时自足表述**，删掉对话痕迹。
- 不能且无事实 → 整段删除。

## 2. 八类泄漏

1. 失效的会话引用（"上面说过"、"如前所述"、指向对话位置的编号）。
2. PR / 分支视角（"本 PR 新增"、"此分支改动了"）。
3. 变更叙事与版本戳（"以前是"、"不再是"、"这一版"——CHANGELOG 除外）。
4. 评审编排（"评审中被否决"、"按 review 意见改为"）。
5. 向评审者辩解的注释（"这样写是因为评审要求"）。
6. 控制流 / 推导过程复述（"先查 A 再查 B 所以…"）。
7. 犹豫措辞与无主延期（"也许以后"、"暂时先这样"而无去向——有明确去向的应记入 `docs/TECH_DEBT.md`）。
8. 未完成翻译的残片、无意义的中英混杂。

## 3. 不可误删（HEAD 读者可解析的事实）

issue / ADR / TECH_DEBT 引用（`#7`、`ADR-0002`）、`// ignore:` 的抑制理由、回归锚定（"防止 #3 复发"）、实测边界数据、运行时状态机的新旧状态切换描述、外部标准引用。

## 4. 流程

1. 显式 scope；排除生成物、`docs/adr/`、`docs/TECH_DEBT.md`、归档笔记、`CHANGELOG.md`。
2. 只读审计：`rg` 常见模式（`本 PR|以前是|不再是|评审|如前所述|暂时`）+ 无模式通读散文最密的段落。
3. 按源头修复：生成物先改源注解 / ARB 再生成；`docs/` 与代码注释同步改。
4. 删除前枚举该段的命题，核对第 3 节清单；警惕过度修剪——把义务改成陈述、把假设升级成已发布行为，都是缺陷。
5. 验证：重跑搜索确认只剩合规保留项；`git diff --check`；涉及 .dart 跑 `dart format` + `flutter analyze`。
