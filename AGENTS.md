# AGENTS

## Workflows

### Setup
- Install dependencies: `flutter pub get`

### Run
- Start the app: `flutter run`
- Run on a specific device: `flutter run -d <device-id>`

### Build (Release)
- Android APK: `flutter build apk --release`
- iOS: `flutter build ios --release`

## Notes
- Flutter SDK >= 3.10.8 and Dart SDK >= 3.10.8 are required (see README).
- TODO: Add test/lint commands if they are introduced beyond README guidance.
